
#include <stdio.h>

struct ListNode {
  int val;
  struct ListNode* next;
};

struct ListNode* detect_cycle(struct ListNode* head) {
  if (head == NULL) {
    return NULL;
  }

  return NULL;
}

int main(int argc, char** argv) {
  return 0;
}
